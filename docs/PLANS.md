Update board to hold not only pieces but logs of turns they moved on, and where they moved from

Update piece so that they are no longer represented as an int but as a piece object that already has all their legal moves
